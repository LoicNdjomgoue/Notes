public class string_methods {

    public static void main(String[] args) {
        
        //String = a reference data type that can store on or more character reference data types have access to useful methods

        String name = "loic";

        //boolean result = name.equals("Loic");
        //int result = name.length();
        //char result = name.charAt(0);
        //int result = name.indexOf("c");
        //boolean result = name.isEmpty();
        String result = name.toUpperCase();
        //String result = name.toLowerCase();
        //String result = name.trim();
        //String result = name.replace('o', 'b');

        System.out.println(result);


    }
}